{% extends 'base.html.twig' %}

{% block title %}ImageCommunityManager{% endblock %}

{% block body %}
    <h1>ImageCommunityManager</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image_community_manager.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_image_community_manager_index') }}">back to list</a>

    <a href="{{ path('app_image_community_manager_edit', {'id': image_community_manager.id}) }}">edit</a>

    {{ include('image_community_manager/_delete_form.html.twig') }}
{% endblock %}
