{% extends 'base.html.twig' %}

{% block title %}ImageCM index{% endblock %}

{% block body %}
    <h1>ImageCM index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom_image</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for image_c_m in image_c_ms %}
            <tr>
                <td>{{ image_c_m.id }}</td>
                <td>{{ image_c_m.nomImage }}</td>
                <td>
                    <a href="{{ path('app_image_c_m_show', {'id': image_c_m.id}) }}">show</a>
                    <a href="{{ path('app_image_c_m_edit', {'id': image_c_m.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_image_c_m_new') }}">Create new</a>
{% endblock %}
