<form method="post" action="{{ path('app_message_community_manager_delete', {'id': message_community_manager.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message_community_manager.id) }}">
    <button class="btn">Delete</button>
</form>
